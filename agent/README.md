# Forge Agent

This workspace is reserved for the future self-developed Forge agent.

Keep agent runtime code, protocol implementation, tests, and packaging here so it can evolve independently from the existing CLI client in `../cli`.

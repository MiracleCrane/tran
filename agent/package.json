{
  "name": "@claude-forge/agent",
  "version": "0.1.0",
  "private": true,
  "description": "Anvil — Forge's self-developed agent runtime. Provider-direct, SDK-free agent loop for code + daily tasks.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  }
}
